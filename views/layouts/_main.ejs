<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof title !== 'undefined' ? title : (company?.name || 'Engage') %></title>

  <link rel="icon" href="<%= company?.branding?.logoUrl || '/img/favicon.png' %>">

  <!-- Bootstrap 5 CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  >

  <%
    const primaryColor =
      (company && company.branding?.theme?.primary) || '#0a66c2';
    const prefersDark = false;
    const isAuth = (typeof auth !== 'undefined' ? !!auth : false);
  %>

  <style>
    :root {
      --bs-primary: <%= primaryColor %>;
    }

    body {
        background-color: #e9ecef;  /* Bootstrap’s “gray-200”, a soft light gray */
        color: <%= prefersDark ? '#eee' : '#222' %>;
        font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
        margin: 0;
        min-height: 100vh;
        }

    /* Limit auth card width */
    .auth-container {
      width: 100%;
      max-width: 360px;
    }

    .auth-card {
      background: #fff;
      border: none;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }

    .auth-logo img {
      height: 44px;
      width: auto;
      border-radius: 8px;
    }

    .auth-title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-top: 12px;
    }

    .auth-subtle {
      font-size: 0.9rem;
      color: #666;
      margin-bottom: 1.2rem;
    }

    footer {
      font-size: 0.85rem;
      color: #888;
    }
  </style>
</head>

<body>
  <% if (isAuth) { %>
    <!-- ================= AUTH PAGE ================= -->
    <div class="d-flex flex-column justify-content-center align-items-center min-vh-100">
      <div class="auth-container text-center">
        <div class="auth-logo mb-2">
          <img src="<%= company?.branding?.logoUrl || '/img/favicon.png' %>" alt="<%= company?.name || 'Engage' %>">
        </div>
        <h1 class="auth-title">Welcome back 👋</h1>
        <p class="auth-subtle">Log in to keep your team in the loop.</p>

        <div class="card auth-card">
          <div class="card-body p-4">
            <%- include('../partials/flash') %>
            <%- body %>
          </div>
        </div>

        <footer class="mt-3">
          © <%= new Date().getFullYear() %> <%= company?.name || 'SunTec' %>
        </footer>
      </div>
    </div>

  <% } else { %>
    <!-- ================= APP LAYOUT ================= -->
    <main class="d-flex flex-column justify-content-center align-items-center min-vh-100">
      <div class="container" style="max-width: 500px;">
        <%- include('../partials/flash') %>
        <%- body %>
      </div>
    </main>

    <footer class="border-top text-center py-3">
      © <%= new Date().getFullYear() %> <%= company?.name || 'Engage' %>
    </footer>
  <% } %>

  <!-- Bootstrap JS Bundle -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous">
  </script>
</body>
</html>
