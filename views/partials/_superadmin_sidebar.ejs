<style>
    .sa-sidebar {
  background: var(--paper-2);
  border-radius: var(--radius);
  padding: 16px 14px;
  border: 1px solid rgba(15, 23, 42, 0.06);
}

.sa-sidebar-header {
  margin-bottom: 12px;
}

.sa-sidebar-title {
  font-size: 13px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--ink-500);
}

.sa-sidebar-nav {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sa-sidebar-nav li {
  margin-bottom: 4px;
}

.sa-sidebar-nav a {
  display: block;
  padding: 8px 10px;
  border-radius: 999px;
  font-size: 14px;
  color: var(--ink-700);
  text-decoration: none;
}

.sa-sidebar-nav a:hover {
  background: rgba(15, 23, 42, 0.04);
}

.sa-sidebar-nav li.is-active a {
  background: rgba(60, 164, 107, 0.08);
  color: var(--jaango-green);
  font-weight: 600;
}
</style>
<nav class="sa-sidebar">
  <div class="sa-sidebar-header">
    <span class="sa-sidebar-title">Super Admin</span>
  </div>

  <ul class="sa-sidebar-nav">
    <li class="<%= currentSection === 'dashboard' ? 'is-active' : '' %>">
      <a href="/super-admin/home">Dashboard</a>
    </li>

    <li class="<%= currentSection === 'companies' ? 'is-active' : '' %>">
      <a href="/super-admin/companies">Companies</a>
    </li>

    <li class="<%= currentSection === 'pending-companies' ? 'is-active' : '' %>">
      <a href="/super-admin/companies/pending">Pending verification</a>
    </li>

    <!--
    Future items:
    <li class="<%= currentSection === 'users' ? 'is-active' : '' %>">
      <a href="/super-admin/users">Users</a>
    </li>

    <li class="<%= currentSection === 'usage' ? 'is-active' : '' %>">
      <a href="/super-admin/usage">Usage & Storage</a>
    </li>

    <li class="<%= currentSection === 'system' ? 'is-active' : '' %>">
      <a href="/super-admin/system/mailer">System / Mailer</a>
    </li>

    <li class="<%= currentSection === 'audit' ? 'is-active' : '' %>">
      <a href="/super-admin/logs/audit">Audit Logs</a>
    </li>
    -->
  </ul>
</nav>
