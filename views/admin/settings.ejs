<% layout('layouts/_layout') %>

<section style="max-width:900px;margin:24px auto;display:grid;gap:16px;">
  <h2 style="margin:0;">Admin Settings</h2>

  <!-- Admin Quick Nav -->
  <div class="admin-pills" style="display:flex;gap:8px;flex-wrap:wrap;margin:8px 0 16px;">
    <a href="/<%= company.slug %>/admin/internal-links"
       class="pill-link <%= (typeof currentPath !== 'undefined' && currentPath.includes('/admin/internal-links')) ? 'active' : '' %>">
       🔗 Internal Links
    </a>
    <a href="/<%= company.slug %>/admin/perf"
       class="pill-link <%= (typeof currentPath !== 'undefined' && currentPath.includes('/admin/perf')) ? 'active' : '' %>">
       ⚡ Performance Logs
    </a>
    <a href="/<%= company.slug %>/admin/audit"
       class="pill-link <%= (typeof currentPath !== 'undefined' && currentPath.includes('/admin/audit')) ? 'active' : '' %>">
       🧾 Audit Logs
    </a>
  </div>
  


  <!-- ONE MAIN FORM -->
  <form action="/<%= company.slug %>/admin/settings" method="POST" style="display:grid;gap:18px;">
    <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

    <!-- Identity -->
    <div class="card" style="padding:16px;display:grid;gap:12px;">
      <h3 style="margin:0;">Identity</h3>

      <label style="display:block;">
        <div style="margin-bottom:6px;">Product Name (internal brand)</div>
        <input name="productName" type="text"
               value="<%= company.productName || '' %>"
               placeholder="e.g., SunVerse / Pulse / Loop"
               style="width:100%;padding:8px;border:1px solid #ddd;border-radius:8px;">
      </label>

      <label style="display:block;">
        <div style="margin-bottom:6px;">Tagline (optional)</div>
        <input name="tagline" type="text"
               value="<%= company.tagline || '' %>"
               placeholder="Where our people connect & celebrate"
               style="width:100%;padding:8px;border:1px solid #ddd;border-radius:8px;">
      </label>
    </div>

    <!-- Branding -->
    <div class="card" style="padding:16px;display:grid;gap:12px;">
      <h3 style="margin:0;">Branding</h3>

      <div style="display:flex;gap:16px;align-items:center;flex-wrap:wrap;">
        <label>
          <div style="margin-bottom:6px;">Primary Color</div>
          <input name="themePrimary" type="color"
                 value="<%= company.branding?.theme?.primary || '#0a66c2' %>">
        </label>

        <label>
          <div style="margin-bottom:6px;">Accent Color</div>
          <input name="themeAccent" type="color"
                 value="<%= company.branding?.theme?.accent || company.branding?.theme?.primary || '#0a66c2' %>">
        </label>

        <div style="margin-left:auto;display:flex;align-items:center;gap:8px;">
          <div>Preview:</div>
          <div title="Primary"
               style="width:24px;height:24px;border-radius:6px;background:<%= company.branding?.theme?.primary || '#0a66c2' %>;"></div>
          <div title="Accent"
               style="width:24px;height:24px;border-radius:6px;background:<%= company.branding?.theme?.accent || company.branding?.theme?.primary || '#0a66c2' %>;border:1px solid #ddd;"></div>
        </div>
      </div>

      <label style="display:block;">
        <div style="margin-bottom:6px;">Logo URL</div>
        <input type="url" name="logoUrl" placeholder="https://..."
               value="<%= company.branding?.logoUrl || '' %>"
               style="width:100%;padding:8px;border:1px solid #ddd;border-radius:8px;">
      </label>

      <% if (company.branding?.logoUrl) { %>
        <div style="display:flex;align-items:center;gap:12px;">
          <div style="color:#666;font-size:13px;">Current Logo:</div>
          <img src="<%= company.branding.logoUrl %>" alt="logo" style="height:36px;max-width:160px;object-fit:contain;">
        </div>
      <% } %>
    </div>

    <!-- Policies -->
    <div class="card" style="padding:16px;display:grid;gap:12px;">
      <h3 style="margin:0;">Policies</h3>

      <div style="margin:4px 0 10px;">
        <div style="margin-bottom:6px;">Posting Mode</div>
        <label style="margin-right:16px;">
          <input type="radio" name="postingMode" value="OPEN"
                 <%= company.policies?.postingMode === 'OPEN' ? 'checked' : '' %> >
          Open — publish instantly
        </label>
        <label>
          <input type="radio" name="postingMode" value="MODERATED"
                 <%= company.policies?.postingMode === 'MODERATED' ? 'checked' : '' %> >
          Moderated — approval required
        </label>
      </div>

      <!-- Email Notifications -->
      <div style="margin-top:8px;padding-top:8px;border-top:1px dashed #e5e5e5;">
        <div style="margin-bottom:6px;">Email Notifications</div>
        <label style="display:flex;align-items:center;gap:8px;">
          <input type="checkbox" name="notificationsEnabled" value="1"
                 <%= company.policies?.notificationsEnabled !== false ? 'checked' : '' %> />
          <span>Enable emails for post approvals and @mentions</span>
        </label>
        <div style="color:#666;font-size:13px;margin-top:4px;">
          Sent from the central mailer, branded with your logo and colors.
        </div>
      </div>

      <label style="display:block;">
        <div style="margin-bottom:6px;">Blocked Words (comma-separated)</div>
        <textarea name="blockedWordsCSV" rows="3"
                  style="width:100%;padding:8px;border:1px solid #ddd;border-radius:8px;"><%= (company.policies?.blockedWords || []).join(', ') %></textarea>
      </label>

      <label style="display:block;">
        <div style="margin-bottom:6px;">Retention Days (7–3650)</div>
        <input name="retentionDays" type="number" min="7" max="3650"
               value="<%= company.policies?.retentionDays || 730 %>"
               style="width:180px;padding:8px;border:1px solid #ddd;border-radius:8px;">
      </label>
    </div>

        <!-- Plan & Usage (read-only) -->
    <div class="card" style="padding:16px;display:grid;gap:8px;">
      <h3 style="margin:0;">Plan & Usage</h3>
      <div>Plan: <strong><%= company.planState || 'FREE_TRIAL' %></strong>
        <% if (company.trialEndsAt) { %>
          &nbsp;•&nbsp; Ends: <strong><%= new Date(company.trialEndsAt).toDateString() %></strong>
        <% } %>
      </div>
      <div>Seats:
        <strong><%= (company.license?.used || 0) %></strong> /
        <strong><%= (company.license?.seats || 25) %></strong>
        <% if (company.license?.validTill) { %>
          &nbsp;•&nbsp; Valid till: <strong><%= new Date(company.license.validTill).toDateString() %></strong>
        <% } %>
      </div>
      <div style="margin-top:8px;">
        <form method="POST" action="/<%= company.slug %>/admin/support/upgrade">
          <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
          <button class="btn" type="submit">Request upgrade</button>
        </form>
      </div>
    </div>


    <div style="display:flex;gap:8px;justify-content:flex-end;">
      <button class="btn" type="submit">Save Settings</button>
    </div>
  </form>

  <!-- Utilities (separate, NOT nested) -->
  <div class="card" style="padding:16px;display:flex;flex-wrap:wrap;gap:8px;align-items:center;justify-content:space-between;">
    <div style="font-weight:600;">Utilities</div>
    <div style="display:flex;gap:8px;flex-wrap:wrap;">
      <!-- JSON Backup -->
      <form method="GET" action="/<%= company.slug %>/admin/export/backup.json">
        <button class="btn" type="submit">⬇️ Download JSON Backup</button>
      </form>

      <!-- NDJSON Backup -->
      <form method="GET" action="/<%= company.slug %>/admin/export/backup.ndjson">
        <button class="btn" type="submit">⬇️ Download NDJSON Backup</button>
      </form>

      <!-- Run Retention -->
      <form method="POST" action="/<%= company.slug %>/admin/retention/run" onsubmit="return confirm('Run retention now? This will hard-delete expired soft-deleted items.');">
        <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
        <button class="btn danger" type="submit">🗑️ Run Retention</button>
      </form>
    </div>
  </div>
</section>
